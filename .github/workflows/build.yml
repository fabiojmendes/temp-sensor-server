name: Go

on:
  push:
    branches: [ main ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
#       with:
#         go-version: v1

    - name: Build Producer
      run: go build -v ./src/tsproducer
